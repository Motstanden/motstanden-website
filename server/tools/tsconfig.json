{
    "extends": "../tsconfig.json",
    "compilerOptions": {
		"baseUrl": "./",
		"outDir": "./build", 	/* Specify an output folder for all emitted files. */
		"typeRoots" : [
			"../node_modules/@types"
		]
    },
    "exclude": [
    	"../node_modules",
		  "build/**/*"
    ],
    "include": [
      	"./src/**/*"
    ]
}