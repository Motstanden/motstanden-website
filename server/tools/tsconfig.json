{
    "extends": "../tsconfig.json",
    "compilerOptions": {
		"baseUrl": "./",
		"outDir": "./dist", 	/* Specify an output folder for all emitted files. */
		"typeRoots" : [
			"../node_modules/@types"
		]
    },
    "exclude": [
    	"../node_modules"
    ],
    "include": [
      	"./src/**/*"
    ]
}