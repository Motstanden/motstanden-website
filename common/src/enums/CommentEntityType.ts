
// All though a purist would argue the strings should be singular, 
// the plural forms are used to match the API endpoints - which is very convenient!

export enum CommentEntityType {
    Event = "events",
    Poll = "polls",
    SongLyric = "lyrics",
    WallPost = "wall/posts"
}